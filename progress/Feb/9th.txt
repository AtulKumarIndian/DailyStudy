I woke up 6:30 
studies 1 q in graph - Cheapest Flight 

k stop with min dist.. we use the dist array and pq - [stop, node]
1. build HashMap with node - > (node, cost)

Map<Integer, List<int[]>> graph = new HashMap<>();
        for (int[] flight : flights) {
            graph.computeIfAbsent(flight[0], key -> new ArrayList<>()).add(new int[]{flight[1], flight[2]});
        }

2.   Use queue as stops increase linear.. 

Queue<Tuple> q=new LinkedList<>();   // not PQ


q.offer(new Tuple(stop+1, nei, cost[val]) );



3. 👉 Relax cost like Dijkstra
        int[] cost = new int[n];
        Arrays.fill(cost,Integer.MAX_VALUE);
        cost[src]=0;


          if(costP+costnei < cost[nei] && stop<=k ){
                    dist[nei]= costP+costnei ;
                    q.offer(new Tuple(stop+1,val,dist[nei]));
                }
